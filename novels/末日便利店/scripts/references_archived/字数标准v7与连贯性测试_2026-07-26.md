# 字数标准 v7 + 连贯性测试模式（2026-07-26 焊死）

> **触发**：用户原话"每章定个 3000 字上下吧" → 起点 + 番茄统一 3000±20% = 2400-3600 字灵活区间。
> 配套产出：《末日降临，我的便利店禁止打烊》完整 4 卷 120 章大纲 + scripts/consistency_test.py 6 项检测。

---

## 一、字数标准 v7（2026-07-26 用户钦定）

| 项 | v7 当前 | v4 历史 | 备注 |
|---|---|---|---|
| **默认区间** | 2400-3600 字（3000±20%） | 4000-4400 字 | v4 锁死作废 |
| **用户语义** | "3000 字上下" = ±20% 灵活 | "4000 字以内" 误解为硬卡 | 永远按灵活区间算 |
| **max_tokens** | `max(6000, wc_max*4+1000)` = 14400 | 同前 18000 | wc_max=3600 |
| **覆盖平台** | 起点 + 番茄统一 | 同前 | 双发同字数 |

**铁律（2026-07-26 焊死）**：
- 用户说"X 左右/上下/以内" → X±20% 灵活区间
- ❌ 永远不硬卡字数（"X 字" ≠ "正好 X 字"）
- ✅ commit.msg 字数必须实测（参考 `砍字与新书开坑实战_2026-07-26.md` 第 3 坑）

**v4 → v7 切换时间线**：
| 版本 | 区间 | 用户原话 |
|---|---|---|
| v1 | 起点 1500-2500 / 番茄 800-1000 | "先试试水" |
| v2 | 起点 1800-2500 / 番茄 1000-1200 | "短一点也能签约" |
| v3 | 双 2500-3500 | "字数每章改成 4000 左右吧"（误解） |
| v4 | 双 4000-4400 | 同上锁死 |
| **v5/v6** | 2500-3000 / 3000-3500 短版 | 番茄 Ch4/Ch7 短版回退 |
| **v7（当前）** | **双 2400-3600（3000±20%）** | **"每章定个 3000 字上下吧"** |

---

## 二、连贯性测试 6 项检测（本会话 9b38d01 commit 验证）

**触发**：长篇项目 > 20 章必须配套 `scripts/consistency_test.py`，跑全 6 项。

| 检测 | 算法 | 阈值 | 实测 |
|---|---|---|---|
| **字数合规** | `re.findall(r'[\u4e00-\u9fff]', body)` | 2400-3600/章 | ✅ Ch1-Ch3 全过 |
| **伏笔一致性** | 解析 outline 表格 → 验证 `planted < redeemed` | 埋入 < 回收 ≤ 120 | ✅ 46 条无矛盾 |
| **数字一致性** | 锁死关键数字（卷一 14 人/收容队 47/倒计时 47） | 不出反向数字 | ✅ 无矛盾 |
| **人物一致性** | 角色名 × 章号矩阵 | 出场次数合理 | ✅ 林默 3 + 周磊 3 + 那个男孩 2 |
| **规则一致性** | 6 条原文 + 反向变体黑名单 | 原文不被改 | ✅ 无破坏 |
| **章末钩子** | 「第N章 完」+「末段自检」必带 | 必带 | ✅ 3/3 |

**脚本骨架**（实战参考 `Riky-/novels/末日便利店/scripts/consistency_test.py` 230 行）：

```python
#!/usr/bin/env python3
import re, os, sys
from pathlib import Path

CHAPTERS_DIR = Path(sys.argv[1] if len(sys.argv) > 1 else "chapters")
OUTLINE_FILE = CHAPTERS_DIR.parent / "outline" / "设定.md"
# 兜底：脚本移位后从 sibling 找
if not CHAPTERS_DIR.exists():
    CHAPTERS_DIR = Path(__file__).parent.parent / "chapters"
if not OUTLINE_FILE.exists():
    OUTLINE_FILE = Path(__file__).parent.parent / "outline" / "设定.md"

WC_MIN, WC_MAX = 2400, 3600
TARGET_CHAPTERS = 120

def count_chinese(text):
    body_end = text.find('**第')
    for marker in ['**第一章 完**', '**第二章 完**']:
        idx = text.find(marker)
        if idx > 0: body_end = idx
    body = text[:body_end] if body_end > 0 else text
    clean = re.sub(r'^#.*$', '', body, flags=re.M)
    clean = re.sub(r'\n+', '', clean)
    return len(re.findall(r'[\u4e00-\u9fff]', clean))

def check_word_count(): ...
def check_hooks(): ...      # 解析 outline 表格
def check_numbers(): ...    # 锁死卷一 14 人等
def check_characters(): ... # 关键角色出场矩阵
def check_rules(): ...      # 6 条原文 + 黑名单变体
def check_end_hooks(): ...  # 中文章号转换 + 粗体兼容
```

**5 个踩坑 + 修复**（本会话实战）：
1. **路径兜底** — `Path(__file__).parent.parent` 兼容脚本移位
2. **中文章号转换** — `ch_n = int(m.group(1))` + `cn_nums = "〇一二三四五六七八九十"` 拼"第N章"
3. **粗体兼容** — 检测 `**第N章 完**` / `第N章 完` / `第N章完` 三种格式
4. **伏笔 regex** — 用 raw string `r'\| (F\d+) \| (.+?) \| Ch(\d+) \| Ch(\d+) \|'` 避免转义
5. **占位行过滤** — `if '—' in content or '-' in content[:3]: continue` 跳过表格里的"—"占位行

---

## 三、30 万字完整大纲 7 块必备（本会话《末日便利店》验证版）

```markdown
# 设定 v3 模板（30万字 / 4卷 / 120章）

## 类型 / 平台
## 一句话卖点
## 番茄简介（300字）
## 番茄标签（5-8个）
## 卷一 → 卷四 整体走向（表格：4 行）
## 完整 4 卷 / 120 章大纲（每章：标题 + 主线 + 字数）
## 完整人物谱（30+ 人：身份 + 出场章 + 结局）
## 完整道具表（30+ 件：首次章 + 作用 + 状态）
## 完整伏笔表（50+ 条：F001-F050+，埋入 + 回收 + 状态）
## 8+3+3+1 规则（核心 8 + 卷三新增 3 + 超市反规则 3 + 主角第三条 1）
## 5 级系统升级路径（表格）
## 数字一致性锁死（关键数字 + 出现章）
## 当前进度（✅ / ⏳ 标记）
```

**铁律（30 万字规模 5 件必交付）**：
1. **120 章大纲** — 每章都有"标题 + 主线 + 字数目标"
2. **30+ 人物谱** — 每人"身份 + 出场章 + 结局"
3. **30+ 道具表** — 每件"首次章 + 作用 + 状态"
4. **50+ 伏笔表** — 每条"埋入章 + 回收章 + 状态"
5. **连贯性测试脚本** — 6 项检测全绿

---

## 四、本会话《末日降临，我的便利店禁止打烊》开坑实证

**完成产物**：
| 文件 | 路径 | 规模 |
|---|---|---|
| 设定 v1 | `novels/末日便利店/outline/设定.md` | 5029 字节（10 章细纲） |
| 设定 v2 | 同上 | 9561 字节（+道具/伏笔/人物） |
| 设定 v3 | 同上 | **24478 字节**（120 章 + 33 人 + 36 道具 + 50 伏笔） |
| Ch1 v6 | `chapters/末日便利店_第1章_死在门口的男人.md` | 3599 汉字 ✅ |
| Ch2 | `chapters/末日便利店_第2章_找零不能给.md` | 2821 汉字 ✅ |
| Ch3 | `chapters/末日便利店_第3章_23点的客人.md` | 2755 汉字 ✅ |
| 连贯性测试 | `scripts/consistency_test.py` | 230 行 |

**核心剧情设定**：
- 类型：末日经营 + 规则怪谈 + 轻系统
- 平台：番茄
- 主角：林默（27 / 督导被裁 / 18万网贷 / 继承便利店）
- 核心规则：禁止暴力 / 等价交换 / 23点非人时段 / 污染等级 / 自行定价 / 概不赊账
- 金手指：安全区经营系统 Lv.1-Lv.5
- 卷一主线：店铺继承 → 规则浮现 → 第一个员工 → 系统创建者现身
- 卷四主线：反派真相 → 系统创建者救赎 → 末日结束/重启

---

## 五、与已有 reference 的关系

| Reference | 主题 | 与本文件关系 |
|---|---|---|
| `砍字与新书开坑实战_2026-07-26.md` | Ch1 砍字 SOP + 7 步开坑 | 互补：实战砍字细节 |
| `开新书设定模板_2026-07-26.md` | 30 章设定模板 | 互补：本文件扩展到 120 章 |
| `扩字模式_v6_对话回声病_2026-07-07.md` | 对话回声病 | 不变 |
| `扩字模式_破折号堆与剧情错位_2026-07-06.md` | 破折号堆 | 不变 |
| `扩字模式_汉字口径_数字单调性_2026-07-06_v4.md` | 扩字精度 | 不变 |
| `chapter_expansion_precision_v3_2026-07-05.md` | 扩字精度 v3 | 不变 |

---

## 六、复盘清单（给未来的 DS-0）

**字数控制**：
- [ ] 用户说"X 左右/上下/以内" → X±20% 灵活区间
- [ ] 写完章节立即实测（`re.findall(r'[\u4e00-\u9fff]', body)`）
- [ ] commit.msg 字数 = 实测变量（不是粗估）
- [ ] 三处同步 + git push

**30 万字规模**：
- [ ] 120 章大纲 + 30+ 人物 + 30+ 道具 + 50+ 伏笔 + 8+3+3+1 规则
- [ ] `scripts/consistency_test.py` 6 项检测全绿
- [ ] 数字一致性锁死（卷一 14 人 / 收容队 47 / 倒计时 47）

**连贯性测试脚本 5 踩坑**：
- [ ] 路径兜底（`Path(__file__).parent.parent`）
- [ ] 中文章号转换（`cn_nums = "〇一二三四五六七八九十"`）
- [ ] 粗体兼容（`**第N章 完**` / `第N章 完` / `第N章完`）
- [ ] 伏笔 regex 用 raw string
- [ ] 占位行过滤（`if '—' in content: continue`）

---

## 关联引用

- `chapter-wordcount-precision`（伞技能）
- `砍字与新书开坑实战_2026-07-26.md`（砍字 SOP）
- `开新书设定模板_2026-07-26.md`（30 章设定模板）
- `novelforge-architecture/references/长篇连贯性测试_2026-07-26.md`（跨技能同步）
- `fake_completion_round2_2026-07-04.md`（commit 早于写完反模式）